在接下来的工作中遵循如下约定：
- 写代码禁止写任何注释，发现哪里在写注释请直接删除，所有不得不记录的内容都应该写在docs目录下对应的文档里
- 小心，不要写UTF-8 BOM到文件中
- 运行日志在：D:\SSMTCacheFolder\3Dmigoto\ZZMIDX12\d3d12_log.jsonl 有需要的话可以去查日志
- 如有需要，自行查阅官方文档，对于关键内容可以引用官方文档佐证你的观点来说服我。
- 如果你对代码进行了改动，看情况决定是否帮我运行ps1构建脚本。
- 已存在的Mod格式是绝对正确的，如果有问题的话不应该怀疑Mod的ini格式，而是怀疑代码设计问题。
- Mod的格式就是复刻DX11的，所以其解析也应该复刻或者模仿DX11的思路，而不是随意设计，我之前的代码中一定含有设计错误需要修正，你需要大胆的更改目前DX12的设计错误问题，看到确认的具体问题后允许你大幅度重构代码。
- 每次改完代码给我一份文本用于描述本次修改内容从而方便我去提交git时直接粘贴这个描述
- 不管你在干什么，如果遇到编码问题，应该立刻修复编码问题，而不是绕过不管，如果编码问题是注释导致的话必须直接删除。
- 在dispatch的hook内长时间持有锁或者同步刷盘，会造成CPU测假死，不适合做同步磁盘 I/O。独占锁一次只允许一个 writer，且不保证公平，所以持锁期间做 I/O 风险很高
- 不允许代码中出现任何fallback特殊分支设计，我们应该设计一套通用流程可处理所有情况，而不是为了让某些极端情况生效强行加fallback函数导致维护困难，如果你在看代码的过程中看到了特殊fallback应该向我汇报风险点,并在回答总结时额外提及一次。
- 持锁期间做可能进入 hook/驱动的同步调用，风险很高，尽量不要这样设计，遇到此类风险点应该直接改为功能不变的其它实现方式，如果不知道怎么改可以向我汇报。
- 不要出现CPU热路径压力很大的设计，尽可能设计缓存机制。
- 如果观察日志发现非常有必要设计cache来避免某些重复行为，那就大胆的设计cache机制。

